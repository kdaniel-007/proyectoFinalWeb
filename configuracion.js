$(document).ready(function(){
    $('#formConfiguracion').on('submit', function(e){
        e.preventDefault();
        // Aquí iría el código para guardar la configuración (ej. en localStorage o a un servidor)
        alert('Cambios guardados exitosamente');
    });
});
